" All of these keybinds assume auto-pairs is on

vmap <space>bf di<space>\textbf{<esc>p
" Fat font, easier to hit than b
vmap <space>ff di<space>\textbf{<esc>p

vmap <space>it di<space>\textit{<esc>p
vmap <space>sc di<space>\textsc{<esc>p

vmap <space>rr di<space>{<esc>p
vmap <space>rs di<space>\{<esc>pF{h
vmap <space>ri di<space>{\ <esc>pF{l

vmap <space>inf di<space>\infer[ H]{<esc>pf}a{

" For vimtex plugin
let maplocalleader = ","
